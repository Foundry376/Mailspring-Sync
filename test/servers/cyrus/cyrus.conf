# Cyrus master configuration for the mailsync integration harness: IMAP only.
START {
  recover   cmd="ctl_cyrusdb -r"
  idled     cmd="idled"
}
SERVICES {
  imap      cmd="imapd" listen="143" prefork=1 maxchild=200
}
EVENTS {
  checkpoint cmd="ctl_cyrusdb -c" period=30
}
DAEMON {
}
